<li class="list-group-item">
	{{burger_name}}

	{{#unless devoured}}<button type="button" class="change-devour btn btn-success" data-id="{{id}}" data-devoured="{{devoured}}">
		EAT!
	</button>{{/unless}}
	{{#if devoured}}<button type="button" class="delete btn btn-danger" data-id="{{id}}" data-devoured="{{devoured}}">
		DELETE!
	</button>{{/if}}
</li>
